// A floor so nothing reads thin, a soft aim, and a high ceiling so big, complex
// scandals can run long. The draft self-sizes to the event; expansion only fills to
// the floor rather than padding everything to a flat target.
export const PUBLISH_READY_MIN_WORDS = 1800;
export const PUBLISH_READY_TARGET_WORDS = 2800;
export const PUBLISH_READY_MAX_WORDS = 4500;
